Wrapper client is created to allow for automated retries and authentication

Base client is generated using `openapi-generator-cli generate -i toast-combined-api-v3.yaml -g python -c openapi-config.yaml -o .`